import 'package:flutter/material.dart';

class ModernAppColors {
  static const Color primary = Color(0xFF003366); // Dark Blue
  static const Color accent = Color(0xFFFF6B6B); // Coral Red for highlights
  static const Color textPrimary =
      Color(0xFFE0E0E0); // Light gray for primary text
  static const Color textSecondary =
      Color(0xFFAAAAAA); // Muted gray for secondary text
  static const Color background =
      Color(0xFF121212); // Very dark gray for background
  static const Color cardBackground =
      Color(0xFF1E1E1E); // Slightly lighter dark gray for cards
  static const Color green = Color(0xFF4CAF50); // Standard green for success
  static const Color red =
      Color(0xFFF44336); // Standard red for warnings/errors
}
